// No matter the value of n, the number of operations is always going to be 1.  (Constant Time)
// O(1) is the most efficeint a code is ever going to be. Its almost a straingt line on the graph.



function addItems(n) {
    return n + n;
}

var sum = addItems(10);

console.log(sum);


// The above code runs only once
